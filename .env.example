version: "3.9"

services:
  telecom-triage:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: telecom-triage-agent
    ports:
      - "8000:8000"
    volumes:
      - ./finalproject:/app/finalproject
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
